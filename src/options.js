const GCOPTIONS = {
    interval: 60 * 1000
};

const WORLDOPTIONS = {
    timeRate: 1,
    velocityScale: 0.001, // velocity per s instead of ms
    tileScale: 100
};

export { GCOPTIONS, WORLDOPTIONS };